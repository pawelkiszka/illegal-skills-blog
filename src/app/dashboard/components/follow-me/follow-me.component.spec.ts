import { FollowMeComponent } from "./follow-me.component";

describe('FollowMeComponent', () => {

    let component: FollowMeComponent;

    beforeEach(() => {
        component = new FollowMeComponent();
    });

    it('should ', () => {

    });
});

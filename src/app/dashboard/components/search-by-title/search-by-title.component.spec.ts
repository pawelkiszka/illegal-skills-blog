import { SearchByTitleComponent } from './search-by-title.component';

describe('SearchByTitleComponent', () => {

    let component: SearchByTitleComponent;

    beforeEach(() => {
        component = new SearchByTitleComponent();
    });

    it('should ', () => {

    });
});

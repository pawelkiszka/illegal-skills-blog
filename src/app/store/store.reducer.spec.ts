import { storeReducer, StoreState } from './store.reducer';

describe('storeReducer', () => {

    it('should resolve', () => {

    });

    it('should handle action not supported by reducer', () => {

    });
});

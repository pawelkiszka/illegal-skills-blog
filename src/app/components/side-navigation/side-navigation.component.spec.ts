import { SideNavigationComponent } from "./side-navigation.component";

describe('SideNavigationComponent', () => {

    let component: SideNavigationComponent;

    beforeEach(() => {
        component = new SideNavigationComponent();
    });

    it('should ', () => {

    });
});
